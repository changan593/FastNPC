# FastNPC 环境变量配置模板
# 复制此文件为 .env 并填入真实的配置值

# 必需配置项
# ====================

# 应用密钥（用于 Cookie 签名，必须是随机字符串）
# 生成方法: python -c "import secrets; print(secrets.token_urlsafe(32))"
FASTNPC_SECRET=your-secret-key-here

# OpenRouter API Key（用于 LLM 调用）
# 获取地址: https://openrouter.ai/
OPENROUTER_API_KEY=your-openrouter-api-key-here

# 可选配置项
# ====================

# 管理员用户名（首个注册的该用户名将自动成为管理员）
FASTNPC_ADMIN_USER=admin

# 前端 CORS 来源（多个用逗号分隔）
# 开发环境示例: http://localhost:5173
# 生产环境示例: https://yourdomain.com
FASTNPC_FRONTEND_ORIGIN=http://localhost:5173

# 最大并发数（用于并发任务处理）
FASTNPC_MAX_CONCURRENCY=4

# 调试模式（设置为 1 开启调试输出）
FASTNPC_DEBUG=0

# 项目根目录（通常不需要设置，会自动检测）
# FASTNPC_ROOT=/path/to/FastNPC

